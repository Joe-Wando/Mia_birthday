// "The Sliding Crest" — a classic sliding tile puzzle. No knowledge needed,
// just patience. Edit the message shown once the crest is restored.

export const gridSize = 3;

export const successMessage =
  "The crest reassembles itself, tile by tile, like it was never really broken — just waiting for someone patient enough to put it back.";

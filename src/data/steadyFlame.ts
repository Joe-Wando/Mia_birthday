// "Steady the Flame" — a timing/precision game. Stop the marker inside the
// glowing zone to light each brazier. No knowledge required, just reflexes.

export const totalBraziers = 5;
export const sweepDurationMs = 1600;

// Width of the target zone as a percentage of the bar, shrinking each round.
export const zoneWidths = [26, 22, 19, 16, 14];

export const successMessage =
  "All five braziers are lit. The room feels warmer already — and so, probably, does whoever's watching you do this.";
